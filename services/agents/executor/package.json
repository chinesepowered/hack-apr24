{
  "name": "branch-agent-executor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Branch executor agent — drives a Plan through fork → migrate → verify → PR → image.",
  "scripts": {
    "start": "tsx src/agent.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^4.76.0",
    "zod": "^3.23.8"
  },
  "optionalDependencies": {
    "@guildai/agents-sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
